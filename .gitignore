# Secrets
.env

# Pipeline artifacts (regenerated by every run)
data/pairs.jsonl
data/triplets.jsonl
data/triplets_eval.jsonl
data/eval.json
models/
results/
mteb_results/

# NOTE: data/raw/ (the bundled medical datasets) IS tracked on purpose.
# If the MedEmbed sample makes the repo too big, uncomment:
# data/raw/medembed_triplets.jsonl

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Tools / editors
.ipynb_checkpoints/
.vscode/
.idea/

# HF caches (if created locally)
.cache/

# Local-only walkthrough doc (kept out of the shipped repo)
PIPELINE_WALKTHROUGH.md
